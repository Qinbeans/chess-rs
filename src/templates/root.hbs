<!DOCTYPE html>
<html lang="en">
    <head>
        <title>{{title}}</title>
        <meta name="description" content="{{description}}">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link rel="stylesheet" href="/app.css">
    </head>
    <body class="bg-black w-dvw h-dvh text-white">
        <header>
            <section>Play</section>
        </header>
        <main>
            <section>
                <h1>{{title}}</h1>
                <p>{{description}}</p>
            </section>
        </main>
    </body>
</html>