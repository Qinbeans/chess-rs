{
	"name": "axum-on-workers",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "pnpm tailwindcss -m -i assets/app.css -o public/app.css && wrangler deploy",
		"dev": "pnpm tailwindcss -m -i assets/app.css -o public/app.css && wrangler dev --env dev"
	},
	"devDependencies": {
		"tailwindcss": "^3.4.1",
		"wrangler": "^3.1.2"
	}
}
